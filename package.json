{
  "name": "express-mega-router",
  "version": "1.0.0",
  "description": "Express middleware allowing dynamic routes and middleware.",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "transpile": "babel src --presets babel-preset-es2015 --out-dir dist",
    "prepublish": "npm run transpile"
  },
  "keywords": [
    "express",
    "middleware",
    "dynamic",
    "route",
    "routing"
  ],
  "author": "kts of kettek",
  "license": "GPL-3.0",
  "dependencies": {
    "path-to-regexp": "^1.7.0"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1"
  }
}
